'use client'
import { useCallback, useState } from 'react'
import type { Coordinates, GeolocationError, GeolocationOptions } from '../lib/types'
export function useGeolocation(options: GeolocationOptions = {}) { const [coords, setCoords] = useState<Coordinates | null>(null); const [loading, setLoading] = useState(false); const [error, setError] = useState<GeolocationError | null>(null); const getLocation = useCallback(() => { if (typeof navigator === 'undefined' || !navigator.geolocation) return setError({ code: 'NOT_SUPPORTED', message: 'Geolocalización no soportada' }); if (location.protocol !== 'https:' && location.hostname !== 'localhost') return setError({ code: 'HTTPS_REQUIRED', message: 'La geolocalización requiere HTTPS' }); setLoading(true); setError(null); navigator.geolocation.getCurrentPosition(p => { setCoords({ latitude: p.coords.latitude, longitude: p.coords.longitude, accuracy: p.coords.accuracy }); setLoading(false) }, e => { const messages: Record<number, GeolocationError> = { 1: { code: 'PERMISSION_DENIED', message: 'Permiso de ubicación denegado' }, 2: { code: 'POSITION_UNAVAILABLE', message: 'Ubicación no disponible' }, 3: { code: 'TIMEOUT', message: 'Tiempo de espera agotado' } }; setError(messages[e.code] ?? { code: 'UNKNOWN', message: e.message }); setLoading(false) }, { timeout: 10000, enableHighAccuracy: false, maximumAge: 60000, ...options }) }, [options.enableHighAccuracy, options.maximumAge, options.timeout]); return { coords, loading, error, getLocation, reset: () => { setCoords(null); setError(null) } } }
