'use client'
import { useCallback, useState } from 'react'
import type { Coordinates, DistanceMatrix, RouteResult, RoutingProvider } from '../lib/types'
export function useRouting(provider: RoutingProvider) { const [route, setRoute] = useState<RouteResult | null>(null); const [loading, setLoading] = useState(false); const [error, setError] = useState<string | null>(null); const calculate = useCallback(async (from: Coordinates, to: Coordinates) => { setLoading(true); setError(null); try { const value = await provider.route(from, to); setRoute(value); return value } catch (e) { const message = e instanceof Error ? e.message : 'No se pudo calcular la ruta'; setError(message); throw e } finally { setLoading(false) } }, [provider]); return { route, loading, error, calculate, recalculate: async () => { throw new Error('Pasa origen y destino a calculate') } } }
export function useDistanceMatrix(provider: RoutingProvider) { const [matrix, setMatrix] = useState<DistanceMatrix | null>(null); const [loading, setLoading] = useState(false); const [error, setError] = useState<string | null>(null); const calculate = useCallback(async (points: Coordinates[]) => { setLoading(true); setError(null); try { const value = await provider.matrix(points); setMatrix(value); return value } catch (e) { const message = e instanceof Error ? e.message : 'No se pudo calcular la matriz'; setError(message); throw e } finally { setLoading(false) } }, [provider]); return { matrix, loading, error, calculate } }
